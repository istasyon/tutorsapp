<nav id="home-navbar" class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#homeNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" id="nav-brand" href="#">PAPIRUS</a>
    </div>
    <div id="homeNavbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <li><a href="#howitworks" class="home-nav-item">Nasıl Çalışır</a></li>
          <% if !current_user || (current_user && current_user.type != "Teacher") %>
            <% if(user_signed_in?) %>
              <li><%= link_to "Eğitmen ol", teacher_path, class:"home-nav-item" %></li>
            <% else %>              
              <li><a href="#" data-toggle="modal" data-target="#ModalLogin" class="home-nav-item" >Eğitmen ol</a></li>
            <% end %>
          <% end %>
          <% if(!user_signed_in?) %>
            <li><a href="#"  data-toggle="modal" data-target="#ModalSignUp" class="home-nav-item" >Kayıt ol</a></li>
            <li><a href="#" data-toggle="modal" data-target="#ModalLogin" class="btn-home-login">Giriş</a></li>
          <% else %>              
            <li><%= link_to "Çıkış", destroy_user_session_path, method: :delete, class:"home-nav-item" %></li>
          <% end %>
        </ul>
    </div> <!-- homeNavbar -->
  </div> <!-- container-fluid -->
</nav> <!-- navbar-fixed-top -->